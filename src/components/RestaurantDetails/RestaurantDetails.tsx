import { RestaurantDetailsTypes } from "../../types/restaurantDetails";
import { StyledWrapper } from "./RestaurantDetails.styled";

export const RestaurantDetails = ({ name }: RestaurantDetailsTypes) => {
  return <StyledWrapper></StyledWrapper>;
};
